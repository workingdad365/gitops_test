apiVersion: apps/v1
kind: Deployment
metadata:
  name: dummy-server
  labels:
    app: dummy-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dummy-server
  template:
    metadata:
      labels:
        app: dummy-server
    spec:
      containers:
        - name: dummy-server
          # TODO: GHCR에 push될 때마다 아래 IMAGE_TAG를 실제 태그로 갱신한다.
          # 예: ghcr.io/workingdad365/gitops_test:20260225-061604
          image: ghcr.io/workingdad365/gitops_test:20260225-061604
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
